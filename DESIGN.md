# SCAI-Agent 網站設計系統規範（DESIGN.md）v1.2.2

> 定調（2026-08-08 人類核定）：**辦公＋AI 分析｜成熟感｜克制的滑鼠互動**——「沉穩情報室」。
> **v1.2.2（2026-08-08 人類核定，指示為「1.＋z-index修掉＋把泡泡出現頻率調到80%」）**：**解除「禁跟隨滑鼠的粒子」禁令**，改為受規範的允許——新增 §6b 游標粒子層（圓點分裂泡泡＋兩級點擊脈衝，z-index 90、軌跡間隔 27.5ms、粒子 ≤96、reduced-motion 與觸控時不啟用軌跡）。§6／§6a 相應改寫。執行者曾提出異議（與可信情報室定位相斥、評審可能讀為 AI slop）並被人類覆核，異議與經過保留於協作紀錄。
> v1.1（2026-08-08 人類核定「全要」A–F）：風格須**靜態一眼可辨**，不得只靠微動效承載——新增深色情報室 chrome（§1a）、側欄辦公佈局（§3a）、等寬數據字（§2）、加大懸浮幅度（§6）、工程網格底紋（§1a）。
> **v1.2.1（2026-08-08 人類「都確認」）**：`--muted` 由 `#8b867d` 加深為 `#736e66`（淺底 3.4:1 → 4.8:1，通過 WCAG AA；Chart.js 座標軸與圖說色同步）；介面標籤全面中文化（§7a）；列印鈕與雙模型警示列的 emoji 改為線性 SVG（`.ic`），DESIGN 黑名單第 5 條「Emoji 當設計元素」自此全站零違規。
> **v1.2（2026-08-08 人類核可 preview9）：定位收斂為「Executive AI Intelligence Workspace／企業級 AI 戰略情報工作台」**——像策略顧問的決策簡報，不像行銷 Landing Page；AI 分析感來自資料結構、互動回饋與細節，不靠特效。新增底紋硬規則（§1b）、簡報封面結構（§3b）、光影三層級（§4）、指標聚光（§6a）、區段索引與問題標籤（§7a）；改寫 §1a 深色範圍與 §6 懸浮上限。
> 本檔為公開網站（`src/site_template.html` → `docs/index.html`）的視覺唯一規範。
> **所有 AI（Claude／GPT／Codex）每輪美編前必讀**；修改本檔需人類批准。
> 網站類型判定＝**App UI（資料密集儀表板）**，規則以 App UI 為主。
> 設計目標排序：**可信 ＞ 專業好讀 ＞ 美**（受眾＝競賽評審、教授、組員）。

---

## 1. 色彩系統（60-30-10）

| 角色 | Token | 值 | 用途 |
|---|---|---|---|
| 底（60%） | `--bg` | `#faf9f7` | 頁面底。暖灰＝已摻品牌色相，**禁換純白/純灰** |
| 面 | `--surface` | `#fffefd` | 卡片面 |
| 墨（30%） | `--ink` / `--ink2` / `--muted` | `#211f1c` / `#4a4741` / `#736e66` | 文字三階。**禁純黑 #000** |
| 框線 | `--line` / `--line2` | `#e8e4dd` / `#d6d0c6` | 弱化使用中（見 §4 光影） |
| 品牌（10%） | `--accent` | `#a44a24` 深陶土 | hero 情境字、當週資料點、關鍵 CTA。**專任品牌色，不作資料序列色** |
| 語意 | `--ok` / `--warn` / `--err` | `#2f7050` / `#a97b1f` / `#b03a30` | 升/警/降。與品牌色分離 |
| 五維資料色 | `--c1`〜`--c5` | `#38547a` `#357355` `#b07a28` `#6f5486` `#9c5058` | 圖表維度專用。**CSS 變數與 JS `DC` 陣列必須同步改** |

### 1a. 深色情報室 chrome（v1.2 改寫）

- **深色只用在三處**：左側欄／頂欄（app chrome）、簡報封面的 masthead 橫幅、Executive Snapshot 讀數面板。**主閱讀面一律淺色**（`--surface`）——深色是錨點與框，不是主題。
- *v1.1 原規定：深色範圍＝chrome＋首屏兩張卡（`.hero .card`／`.side`）全深。**v1.2 廢止**，理由：整片深色讓主閱讀面過重，違背「可信 ＞ 專業好讀 ＞ 美」。*
- Token：`--ink-bg #1e1c19`（chrome 底）｜`--ink-surface #24221e`（hero 卡面）｜`--ink-line #35322b`／`--ink-line2 #413d35`（深底框線）｜`--tx-d #e8e4db`／`--tx-d2 #c6c0b4`／`--mut-d #9a948a`（深底文字三階）｜`--accent-br #cf7a4c`（深底上的品牌亮階）。
- 深底上資料色需提亮（hero 範圍覆蓋 `--c1→#6d8fb8`、`--c3→#c98f39`），語意色亦用亮階（升 `#6fbf95`、降 `#e18f84`）——對比至少 AA。
- **工程網格底紋**：淺色頁面底 `radial-gradient` 點陣（墨色 ≤5% 透明度、22px 間距）；深色 chrome 用白 5%、20px。僅此一種紋理，禁其他裝飾底圖。

### 1b. 影像底紋硬規則（v1.2）

- 唯一允許的裝飾影像＝`scai-header-pattern.png`，且**只出現在簡報封面主區**（`.brief-main`）。
- 必須同時滿足三項：`opacity ≤ .4`、置右（`right -60px center / auto 165%`）、**加左向遮罩**
  `mask-image:linear-gradient(to left,#000 0,rgba(0,0,0,.45) 26%,transparent 52%)`。
- **理由（第 9 輪實測教訓）**：不加遮罩時圖騰圓弧會延伸到主區中央、壓在判定理由段落上。DOM 檢測只能驗證載入與位移量，**測不出視覺干擾**——**底紋類改動一律需人眼或截圖複驗**。

## 2. 字體系統

- **中文**：Noto Sans TC（400/500/600/700/900）。內文 ≥14px、行高 1.65；數字一律 `tabular-nums`。
- **拉丁 Display**：**Space Grotesk**（Google Fonts）——僅用於英文標題（如 hero 的 Crossroads、品牌字 SCAI-Agent）與 KPI 大數字，塑造 AI 分析的幾何精準感。**不用於中文與內文**。
- **等寬數據字（v1.1）**：**IBM Plex Mono**——用於行內數據（`.num` 座標/權重/Δ）、KDF 編號、觸發依據、週次選單，塑造數據終端感。分工：**展示型大數字＝Space Grotesk；行內機讀數據＝Plex Mono**。字族總數 3，已達上限，禁再增。
- 層級靠「字級＋字重＋灰階」三維，同一元素最多用兩維強調（去強調原則）。
- 標題 `text-wrap: balance`；刪節號用 `…` 不用 `...`。
- 內文段落量測寬度封頂 **65ch**。

## 3. 間距與圓角

- 間距一律走 **4px 階梯**（4/8/12/16/20/24/32/40/52），禁任意值（13px、19px 之類）。
- Section 垂直節奏 40px；卡片內距 22/24px。
- **圓角層級**：外層卡 10px、內層元件 6–7px、chip 5px——**禁全站同一大圓角**（AI Slop 黑名單）。內層圓角 ≈ 外層 − 間隙。

### 3a. 側欄辦公佈局（v1.1）

- **≥1100px**：header 轉為**左側固定深色側欄（238px）**，由上而下：品牌 → 週次工具列（prev/選單/next/複製/列印集中一盒）→ 分區導覽（兩組直列，現行項＝左緣 accent 短槓＋淡底）→ 簡略/詳細切換 → 管線狀態貼底。內容區 `padding-left` 讓位。
- **<1100px**：維持頂部深色 sticky 列＋橫向分區導覽（現行為準）。
- 實作紅線：**純 CSS 重排（`display:contents`＋`order`），DOM 與 id 掛勾零改動**；捲動偵測（scrollspy）共用同一套 `.on` 邏輯。

### 3b. 簡報封面結構（v1.2 Hero 定案）

```
.brief（L1 封面卡，radius 12px，--sh3）
├─ .brief-mast   深墨橫幅：§01｜Weekly Strategic Brief｜W{nn}｜資料區間｜【推斷】chip｜雙模型 chip
└─ .brief-body   12 欄
   ├─ .brief-main（span 8，淺色）情境字／白話句／判定理由／雙軸條＋.brief-fx 三層
   └─ .snap（span 4，深墨）Executive Snapshot 讀數面板
.actband（獨立層級）Action Required 標頭 → auto-fit 行動卡 → 前三大 KDF 頁腳
```

- Snapshot 固定七列：情境判定／X 軸／Y 軸／判定信心／雙模型交叉／觸發劇本／資料更新；**無資料一律顯示「—」，禁補值**；底部固定【推斷】聲明。
- `≤960px` 上下堆疊，Snapshot 邊框由 `border-left` 改 `border-top`。
- **系統運行狀態（`#gen`）必須留在 chrome**，不可移入 `#main`——`render()` 會覆寫 `#main` 的 innerHTML，移入即失效。

## 4. 光影（三層級，v1.2 取代單一雙重投影）

| 層級 | 對象 | Token |
|---|---|---|
| L1 | 簡報封面 `.brief` | `--sh3`＝`0 2px 6px rgba(33,31,28,.08), 0 20px 48px rgba(33,31,28,.10)` |
| L2 | 一般卡 `.card`、劇本卡 `.pb` | `--sh2`＝`0 1px 2px rgba(33,31,28,.06), 0 6px 18px rgba(33,31,28,.045)` |
| L3 | 事件卡 `.evc`、決策軌跡 `details`、趨勢摘要 | 無陰影，僅 `1px solid var(--line)` |
| 表列 | 企業畫像 `.pf .it` | 去卡片化：`border-top:2px solid var(--ink)` |

- 卡片**去實線邊框**（或降至 3–5% 色差的極弱框），質感由陰影與規線承擔。
- **硬規則：不得所有區塊同一層級**（AI Slop 判準之一；人類第 9 輪明列「卡片層級差異，不要所有區塊都長得一樣」）。
- 禁厚邊框、禁裝飾性漸層、禁彩色左框（`border-left: 3px solid accent` 為 AI Slop 標誌，第 7 輪已移除）。

## 5. 動態系統

- **時長**：微互動 120–200ms；進場 300–500ms；上限 700ms。
- **緩動**：進場 ease-out、離場 ease-in、位移 ease-in-out。
- **只動 `transform` 與 `opacity`**；禁 `transition: all`。
- **首屏 staggered reveal**：hero 內元素按「情境字 0ms → 白話句 120ms → 判定理由 200ms → 雙軸條 300ms → 行動側欄 380ms」遞進浮現（fade-in-up 8px）。
- 一律包在 `@media (prefers-reduced-motion: no-preference)`。
- 每個動畫都要有目的（狀態、注意力、空間關係）；禁純裝飾動效。

## 6. 滑鼠互動規範（成熟感＝物理回饋，不是花招）

- **卡片 hover（v1.2 收斂上限）**：可互動卡（劇本卡、事件卡）**上浮上限 `translateY(-2px)`**＋框線加深至 `--line2`＋陰影升一級（L2→L3）＋**左上 28px 陶土指示線** `scaleX(0→1)`。180–200ms ease-out。純資訊卡（企業畫像、Snapshot）不動。
  *v1.1 原規定：可互動卡上浮 −4px。**v1.2 廢止**，理由：人類第 9 輪明列「向上移動最多 2px」。*
- **圖表卡不上浮**：只加深框線與放大鈕——上浮會與 Chart.js tooltip 的命中判定互相干擾。
- **按鈕/連結**：全部要有 hover（色/框變化）與 `focus-visible` 環（禁裸 `outline:none`）；**深色 chrome 內 focus 環改用 `--accent-br`**（`--accent` 在深底對比不足）。chip 需具備 hover／active／focus-visible 三態。
- **來源連結**：箭頭 hover 時 `translate(3px,-2px)`。
- **可點的東西要長得可點**：不靠 hover 才能發現（行動裝置無 hover）。`cursor:pointer` 必備。**任何資訊都不得只在 hover 才揭露。**
- **觸控命中區 ≥44px**：以 `@media (hover:none)` 的透明 `::after`（`width/height:max(100%,44px)` 置中）擴大，**桌面視覺尺寸不得改變**。
- 表格列 hover 淡底色；圖表 hover 沿用 Chart.js tooltip（已達標）。
- **動效時長統一 160–240ms**、ease-out。
- **禁**：傾斜 3D 卡、磁吸按鈕、霓虹、發光尾巴、煙火、遊戲 HUD、粒子雨、自訂游標。
  *v1.0–v1.2 原本一併禁止「跟隨滑鼠的光暈／粒子」。**v1.2.2（2026-08-08 人類核定）解除此項禁令**，改為受規範的允許——見 §6b。解除理由由人類裁定；執行者已提出「與可信情報室定位相斥、評審可能讀為 AI slop」之異議並被覆核，異議紀錄保留於《SCAI-Agent_網站美編協作紀錄.md》。*

### 6a. 指標聚光（v1.2）

- 僅作用於 `.brief-main`，三層：底紋位移（上限 ±6px／±4px）、遮罩點陣網格（局部浮現）、極淡陶土光暈（alpha ≤ .05）。
- CSS 變數驅動（`--mx/--my/--px/--py`），`requestAnimationFrame` 節流，離開時平順歸位。
- **雙重把關**：`pointer:fine` 為否、或 `prefers-reduced-motion: reduce` 為真時，**完全不綁定事件**。
- 本層只做底紋位移與局部網格，**不畫粒子**；粒子層見 §6b。

### 6b. 游標粒子層（v1.2.2）

全頁 Canvas 粒子模組（`#scai-pointer-fx`，內嵌無外部依賴），與 §6a 的 hero 聚光並存但職責不同。

- **視覺語言只允許圓點與泡泡**：移動時生成實心圓點，約 26% 生命週期後分裂為 2–3 顆空心泡泡，緩慢擴張、漂移、淡出。**禁長方形、短線、方塊、膠囊形粒子**。
- **點擊回饋分兩級**：一般區域低強度雷達脈衝（11 顆粒子）；按鈕／連結／篩選器／週次切換／圖表為完整決策脈衝（18 顆）。
- 色票僅用本站 token：`--accent #a44a24`、`--ink2 #4a4741`、`--muted #736e66`、`--line2 #d6d0c6`。**禁霓虹與發光。**
- **參數上限**：軌跡生成間隔 **27.5ms**（＝原始 22ms 的 80% 頻率，人類 2026-08-08 核定）、粒子總數 ≤96、DPR ≤2。
- **z-index 必須為 90**——位於內容與 header（50）之上、放大檢視（100）與週次彈窗（110）之下。**不得使用極大值**：否則泡泡會飄在燈箱上方穿幫。
- **必要條件**：`pointer-events:none`（不得攔截任何點擊、hover、tooltip 或文字選取）；`prefers-reduced-motion: reduce` 時完全不啟用；觸控裝置只有點擊脈衝、不產生移動軌跡；靜止時停止 `requestAnimationFrame`；頁面隱藏時清空粒子。

## 7. 文案規則（App UI 語言）

- 工具語言：定位、狀態、行動——不寫情緒行銷語（「歡迎來到…」「解鎖…的力量」即刪）。
- 區塊標題說明「這區是什麼／能做什麼」。
- 按鈕寫具體動作（「跳轉到 W6」不是「確定」）。
- 「刪掉 30% 文案若更好，就繼續刪」。
- 繁體中文；禁中國用語（晶片≠芯片、光罩≠掩膜）。

### 7a. 區段索引與問題標籤（v1.2）

- 八個導覽分區固定編號 **§01–§08**（側欄同步顯示 01–08），簡報封面 masthead 帶 §01。
- 每區標頭必須回答一個問題（`.q`，前綴「問：」），對應人類指定的五問：現在發生什麼／為什麼如此判斷／對欣銓有何影響／應採取什麼行動／依據與不確定性。
- **硬規則：問題標籤與副標（`.sub`）語意不得重複。** 問題交給 `.q`；副標只講**資料範圍／來源／限制／計數**。
  驗收方式：兩者最長共同子字串 **< 4 字**。
- 微標籤（`.mlab` 標準，目前套用於企業畫像指標名 `.pf .k`）：10px 等寬大寫、字距 .11em、顏色 `#6f6a62`。
- **標籤一律繁體中文**（v1.2.1 人類核定）：簡報封面「本週戰略簡報」、讀數面板「決策摘要」。英文標籤僅保留**專有名詞**（`Decision Trace 決策軌跡`、情境英文名 `Crossroads` 等）。
  *原 `Weekly Strategic Brief`／`Executive Snapshot`／`Action Required` 已廢止。行動帶的微標籤直接刪除而非翻譯——譯成中文會與相鄰的「本週行動建議」重複，違反本節去重硬規則。*
- 中文標籤不套 `text-transform:uppercase`（對中文無效），字距降至 .08–.1em，字級提到 11.5–12.5px（10px 中文過小）。

## 8. 負向黑名單（生成時直接禁止）

1. 紫/藍紫漸層背景、藍→紫配色
2. 三等分「圓圈圖示＋粗標題＋兩行說明」功能卡網格
3. 圓圈裡的裝飾圖示、無功能 blob／波浪 SVG 裝飾
4. 全站置中對齊、全站同一大圓角
5. Emoji 當設計元素（標題火箭、emoji 列點）
6. 卡片彩色左框
7. 罐頭 hero 文案
8. 純黑 #000、無色相純灰
9. system-ui / Inter / Roboto 當主字體
10. 為省事把桌面欄直接堆疊當手機版
11. **副標與問題標籤語意重複**（同一句話講兩次）——v1.2 新增
12. **裝飾底紋壓在內文上**（無論多淡）——v1.2 新增

## 9. 憲法紅線（高於本檔，不可被美編動到）

`/*__DATA__*/` 佔位符｜渲染 JS 的 DOM 掛勾｜【推斷】【待公司確認】標示與 footer 聲明｜事件來源連結與 ✓/✗ 觸發依據｜資料數值｜方法論定義文字｜單檔自包含（無打包鏈；外部資源僅 Google Fonts＋cdnjs Chart.js＋本地備援）。詳見《SCAI-Agent_網站美編協作紀錄.md》§3。

## 10. 出處與依據

- Anthropic Claude Cookbook「Prompting for frontend aesthetics」之反收斂原則
- Refactoring UI 方法論（去強調、間距系統、光影）
- gstack design methodology（MIT © 2026 Garry Tan）之 App UI 規則與 AI Slop 黑名單
- OpenAI「Designing Delightful Frontends」AI Slop 清單
- 「AI網站美編設計」NotebookLM（50 來源）三份深度研究報告之七大原則（v1.0 消化結果；第 9 輪該工具讀取失敗，未取用新內容）
- 第 9 輪人類指示原文：Executive AI Intelligence Workspace 定位、滑鼠互動五條、資訊層級八段、紅線九條
- 審核協定見《DESIGN-AUDIT.md》
