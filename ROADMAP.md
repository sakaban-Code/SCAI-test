# SCAI-Agent 後續方向

> 2026-08-24 記錄。決賽 2026-09-02。
> 本檔只記**方向與理由**，不記實作細節——實作決定一律以 repo 內的程式與資料為準。

## 進行中（決賽前）

### A. 一則新聞的完整旅程（現場可操作）
評審自己貼一則新聞，看它穿過系統每一層，每層自陳依據與是否呼叫模型：

| 層 | 依據 | 模型 |
|---|---|---|
| ① 語氣剔除（否定／假設） | `src/daily_watch.py` `strip_untriggerable` | 0 tokens |
| ② 警報分級 RED／YELLOW／GREEN | `data/alert_rules.json` | 0 tokens |
| ③ 72 小時去重 | `logs/alerts_log.csv` | 0 tokens |
| ④ 劇本觸發 | `data/playbook.json` ＋ `plan_engine` | 0 tokens |
| ⑤ 雙軸位移判定 | Cloudflare Worker | **唯一呼叫模型的一層** |

**為什麼**：Demo 50% 的主軸（評審自己操作，不是看播放）；治理 30%（每層指名由哪個檔管）；
Token 20%（六層裡五層零呼叫，從口號變成畫面上看得見的對比）；並直接回答指導教授
2026-08-24 提的「如何進行分析與解讀」。

⚠ **JS 鏡像紀律**：本面板需在瀏覽器端重跑判定，等於同一份邏輯有第三份實作
（`plan_engine.py`／`daily_watch.py`／JS）。**必須附自證**：以建置時的 Python 結果為
基準，頁面載入時用 JS 重跑全部歷史事件並比對，不一致即在畫面上示警。
理由見金庫〈重寫的判定必須自證與正本一致〉與〈自比對的檢查會壞在兩邊是同一邊〉。

### C. 十一週回測時光機
拉時間軸重播每一週：**當週說了什麼 → 後來怎麼了**。
資料已存在（`riskRadar` ＋ `data/risk_outcomes.json` 的已應驗／已化解／追蹤中
＋ 後續週次的實際 X／Y 走向），本項為既有資料的重新呈現，不新增判定。

---

## 預告（決賽後，2026-08-24 由人類指定）

### 1. 每日新聞
每日巡邏的結果目前只存在 `logs/`，網站上看不到。方向是把每日層與每週層在站上並置：
即時偵測（每日）與正式研判（每週）節奏不同、可信度不同，**不可混為一談**——
每日層須明確標示「未經週報四道門檻」。

### 2. 公司各部門對應
把情報訊號對應到企業機能（產銷人發財、品管、資訊），回答「這件事該誰看」。

**注意其前提**：指導教授已指出我們不掌握欣銓各部門現況，因此本項**不得**產出
「某部門應該改善什麼」。可做的是**分派**（這則訊號落在哪個機能的責任範圍），
不是**評價**。分派可由公開資訊推導，評價不行。

### 3. 趨吉避凶：預測未來走向
現行 H+6 風險雷達是列舉風險，尚未給方向性預測。方向是在雙軸上給出帶信賴區間的
走勢推估，並**預先寫下否證條件**（什麼訊號出現代表預測錯了）。

⚠ 前提：預測必須可被事後檢驗，否則只是換句話說的描述。任何預測都要同時產出
「最早可以從哪裡看出它錯了」——這是憲法 LAYER 10 決策四問的第四問。

---

## 已知未解（與上述無關，但賽前要顧）

- `weekly.yml` 的 `git add` 含不存在的 `reports/` 與會誤commit 大量未追蹤預覽檔的 `dashboard/`；管線若首次真跑會出事。
- `src/make_charts.py` 產出的 `charts/*.png` 目前站上無消費者（憲法 LAYER 3 Step 5 仍要求每週四圖，故未刪，待人類裁示）。
- `ALERT_MODE` 仍為 shadow，test → live 兩步待人工操作。
